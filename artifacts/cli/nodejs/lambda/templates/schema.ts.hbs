import { BaseSchema } from "@octopy/serverless-mongoose";

export interface I{{interfaceName}} {
    
}

const schema = new BaseSchema<I{{interfaceName}}>({
    
});

export const {{lambdaModule}}Schema = ['{{lambdaModule}}', schema];