import { JSONSchemaType } from "ajv";

export interface I{{functionName}}Validator {
    
}

export const {{functionName}}Validator: JSONSchemaType<I{{functionName}}Validator> = {
    type: "object",
    properties: {
        
    },
    required: [],
    additionalProperties: false
}